let OrderModel;_fb1‍.x([["addOrder",()=>addOrder],["getOrders",()=>getOrders],["getActivedOrders",()=>getActivedOrders],["getHiddenOrders",()=>getHiddenOrders],["hideOrder",()=>hideOrder],["activateOrder",()=>activateOrder]]);_fb1‍.w("../Models/order",[["default",["OrderModel"],function(v){OrderModel=v}]]);
var randomSentence = require('random-sentence');
 
const getWrap = ( status, msg, json ) =>{
  return ({  
    status: status,
    results: json.length,
    message: msg,
    data: json})
} 

       const addOrder = async (req, res) => {
  
      try {  
        const str  = JSON.stringify(req.body)
        const json = JSON.parse(str)         
      
          const reqCustomer = json.customers[0]
          const customer = {
              city: reqCustomer.city,
              zipCode: reqCustomer.zipCode,
              address: reqCustomer.address,
              email: reqCustomer.email,
              lastName: reqCustomer.lastName,
              firstName: reqCustomer.firstName
          }
      
          const products =  json.products.map( data =>{
              return {
                    footage: data.footage,
                    category: data.category,
                    title: data.title,
                    description: data.description,
                    price:data.price,
                    quantity: data.quantity,
                    img: data.img,
                    repo: data.category
              }
          })
      
            const doc = {
              "customers": [customer],
              "products": products        
            }
      
        const order = new OrderModel(doc)
        order.save()
    
        res.status(200).json({"status":"ok"})   
      } catch (error) {
          res.status(404).json( error)
      }
}

       const getOrders = async (req, res) => {
    try {
      const orders = await OrderModel.find({}).limit(50)
      res.status(200).send( getWrap(200, "get orders", orders) )
    } catch (error) {
      res.status(404).send(error)
    }
}

       const getActivedOrders = async (req, res) => {
  try {
    const orders = await OrderModel.find({hidden:false}).limit(50)
    res.status(200).send( getWrap(200, "get orders", orders) )
  } catch (error) {
    res.status(404).send(error)
  }
}

       const getHiddenOrders = async (req, res) => {
  try {
    const orders = await OrderModel.find({hidden:true}).limit(50)
    res.status(200).send( getWrap(200, "get orders", orders) )
  } catch (error) {
    res.status(404).send(error)
  }
}

       const hideOrder = async (req, res) => {
  try {

    _fb1‍.g.console.log("hide: " + req.params.id)
    _fb1‍.g.console.log("body: " + JSON.stringify(req.body))
      const orders = await OrderModel.findByIdAndUpdate( { _id: req.params.id }, {hidden: true})
      await orders.save()
      res.send(getWrap(200, "hide order", orders))
  } catch (error) {
    _fb1‍.g.console.log(error)
    res.status(404).send(error)
  }
}

       const activateOrder = async (req, res) => {
  try {
    _fb1‍.g.console.log("activation: " + req.params.id)
    _fb1‍.g.console.log("body: " + JSON.stringify(req.body))
      const orders = await OrderModel.findByIdAndUpdate(req.params.id, {hidden: false})
      await orders.save()
      res.send(getWrap(200, "activate order", orders))
  } catch (error) {
    _fb1‍.g.console.log(error)
    res.status(404).send(error)
  }
}



